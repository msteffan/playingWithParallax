// alert("hi")
// jQuery( '.parallax-layer' ).parallax( options );

// $(window).scroll(function(){
//       $('.parallax-layer').each(function(r){
//           var pos = $(this).offset().top;
//           var scrolled = $(window).scrollTop();
//           $('.parallax-layer').css('top', -(scrolled * 0.5) + 'px');
//       });
//   });
